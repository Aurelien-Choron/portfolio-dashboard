__pycache__/
*.pyc
.venv/
venv/
*.log

# Personal data: never version-controlled
data/fortuneo/*.csv
data/trade_republic/*.csv
data/processed/*.csv
data/accounts/
data/corrections/*.csv
config/exposure.json
config/tickers.json
config/fees.json
config/asset_classes.json
config/target_allocation.json
# (demo/data/... and demo/config/... are NOT affected by these rules: anchored
# to the repo root, they only apply to data/ and config/ themselves.
# demo/ intentionally holds fictional equivalents, committed for the public demo.)

# Market price cache (market_data.py): regenerated automatically
data/cache/

# demo/: only the "source" files (CSV + config) are committed — the price
# cache and normalized journal a local run regenerates there are not.
demo/data/cache/
demo/data/processed/
